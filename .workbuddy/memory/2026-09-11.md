# 2026-09-11

- 解析了 freshman-orientation（智慧迎新系统）项目代码：Spring Boot 3.2.5 + Java 17 + MyBatis-Plus + Thymeleaf + MySQL(freshman_orientation库) + Spring Security，端口 8080。
- 核心亮点为 AiQaServiceImpl：自研中文分词（N-gram+词典正向最大匹配）+ TF-IDF 向量 + 多策略融合评分（Jaccard 0.3 / 余弦 0.5 / 分类 0.2），并支持 OpenAI 兼容 LLM（当前 qwen-plus，RAG 模式，失败降级本地引擎）。
- 注意：application.yml 中明文存有阿里云 apiKey，建议提醒用户不要提交到公开仓库。
- 修复启动报错 MalformedInputException：target/classes/application.yml 编码损坏（既非有效 UTF-8 也非 GBK，中文注释被写坏），src 下源文件正常。已用源文件覆盖 target 副本修复；如复发需 mvn clean 重建，并检查 IDEA File Encodings 设置为 UTF-8。
- 报错复发（第三次）：确认根因为 .idea/encodings.xml 只给 src/main/java 配了 UTF-8，src/main/resources 走系统默认 GBK，导致 IDEA 每次 Build 拷贝 application.yml 时中文注释被转码成 '?'（58 字节损坏）。已修改 .idea/encodings.xml：给 $PROJECT_DIR$ 和 src/main/resources 均指定 UTF-8，并再次恢复 target 副本。用户需重启 IDEA 让配置生效后重新 Run。
- 编码问题确认已解决（应用成功读到配置并启动 Tomcat）。新报错：Table 'freshman_orientation.ai_knowledge' doesn't exist——库存在但未导表。已用 D:\phpstudy_pro\Extensions\MySQL8.0.12\bin\mysql.exe（实际连接的是本机 MySQL 5.6 服务 MySQL56，root/123456，端口 3306）导入 D:\LinkHub\newpeople\freshman_orientation.sql，17 张表全部建立，ai_knowledge 100 条、sys_user 7 条、campus_building 12 条。本机还装有 Navicat for MySQL 和 hotel_pms 库。
- 新增「DeepSeek问答」功能：DeepSeekChatService（RAG：AiQaService.retrieveContext 检索 Top3 → DeepSeek API）、DeepSeekController（/deepseek-chat 页面 + /api/deepseek/chat、/api/deepseek/status）、deepseek-chat.html（DeepSeek 蓝主题 #4D6BFE，未配置 key 显示引导横幅）、SecurityConfig 放行、导航栏加入口、application.yml 新增 app.ai.deepseek 配置块（apiKey 留空，需用户自行到 platform.deepseek.com 申请）。MVN 离线编译通过（mvn 位于 IDEA plugins/maven/lib/maven3/bin）。
- 模型名修正：deepseek-chat/deepseek-reasoner 已于 2026-07-24 被 DeepSeek 官方下线（调用直接报错），当前有效模型为 deepseek-v4-flash（$0.14/$0.28 每百万token，主力）和 deepseek-v4-pro（约3.1倍价，推理加强，thinking mode 为请求级设置）。已把 DeepSeekChatService 默认值、application.yml、deepseek-chat.html 全部改为 deepseek-v4-flash。

# 2026-09-12
- DeepSeek 问答新增 useRag 开关（app.ai.deepseek.useRag，默认 false）：false=纯大模型自由回答（不检索 ai_knowledge），true=RAG 模式检索 Top3 注入提示词。改动：DeepSeekChatService（新增 useRag 字段 + chat() 分支构建两种系统提示词）、application.yml。编译通过；Maven 增量构建未拷贝资源需手动 cp 同步 target（encodings 修复后已无编码问题）。用户已自行填入 DeepSeek apiKey。
- 聊天上下文持久化改造：①sessionId 由每次刷新重新生成改为存 localStorage（deepseek_session_id / ai_session_id）；②新增历史恢复接口 GET /api/deepseek/history、GET /api/ai/history（按 sessionId 查 ai_chat_history，只取 isUnknown=0，id 倒序 LIMIT 后反转）；③DeepSeekChatService 实现多轮上下文——调 API 前取该会话最近 6 轮成功问答拼入 messages 数组（hutool JSONUtil 序列化）；④两个页面 DOMContentLoaded 时 loadHistory() 恢复记录（标"历史消息"角标），头部新增"清空对话"按钮（换新 sessionId 并清空界面）。编译通过。
- 注意：IDEA 打开的文件旧缓冲区会覆盖磁盘改动（useRag 字段曾丢失），改代码前提醒用户先关闭/保存对应文件。
- AI 问答功能改为需登录：SecurityConfig 中 /ai-chat、/api/ai/**、/deepseek-chat、/api/deepseek/** 由 permitAll 改为 authenticated()（置于 /api/** permitAll 之前，优先匹配生效）；common.html 导航栏两个 AI 入口与浮动迷你聊天窗口整体加 sec:authorize="isAuthenticated()"，脚本 getElementById 加判空防匿名报错。
